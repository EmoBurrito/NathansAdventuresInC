#ifndef MEMORY_H_INCLUDED
#define MEMORY_H_INCLUDED

//Prototype the methods in the header file
int MemoryMap(float); //The name isn't necessary because the header file just cares about the type. The name will be declared in declaration.

#endif // MEMORY_H_INCLUDED
